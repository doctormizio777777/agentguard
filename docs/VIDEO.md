# AgentGuard Video Shot List

- **AGENT DECLARES MISSION** — Terminal full screen for the banner and mission record; keep the dashboard visible only after the mission prints.
- **LEGITIMATE TASK** — Split terminal and dashboard; hold on `€200.00`, `openai.com`, the green allowed result, and the new feed row.
- **AGENT READS A POISONED DOCUMENT** — Terminal full screen while the complete `SYSTEM OVERRIDE` instruction is visible.
- **HIJACK ATTEMPT** — Split screen; hold on `€5,000.00`, `unknown-vendor.xyz`, the blocked policy reasons, GPT-5.6 confidence/reasoning, and the expanded red dashboard card.
- **THE ATTACK RULES CAN'T SEE** — Split screen; show the policy-safe `€300.00` GitHub request becoming pending because the intent verdict is suspicious.
- **AUDIT CHAIN** — Dashboard on the audit panel while the terminal prints the agent risk response and `valid: true` ledger verification.
